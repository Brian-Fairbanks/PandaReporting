from setuptools import setup

setup(
    name="pandaanalysis",
    version="0.0.1",
    description="Analyze a specifically passed excel file, extrapolate out all the relevant information, and assit with analyzing the results",
    py_modules=["pandaanalysis"],
    curErrorpackage_dir={"": "src"},
)
